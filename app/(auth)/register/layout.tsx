import React from 'react'

export const metadata = {
    title: 'Register',
    description: '',
}

function Layout({
    children,
}: {
    children: React.ReactNode
}) {

    return (
        <>
            {children}
        </>
    )
}

export default Layout;